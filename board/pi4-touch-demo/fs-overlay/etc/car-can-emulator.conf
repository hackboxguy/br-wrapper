# car-can-emulator configuration file
# Command-line arguments override these values
# Using vcan0 to avoid collision with real CAN hardware on can0

CAN_NODE=vcan0
TCP_PORT=8080
DEBUG_PRINT=false
BIND_ALL=false
SIMULATE=true
SIMULATE_SPEED=1.0
