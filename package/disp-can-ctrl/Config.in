config BR2_PACKAGE_DISP_CAN_CTRL
    bool "disp-can-ctrl"
    depends on BR2_TOOLCHAIN_HAS_THREADS
    help
      Display CAN Control daemon with TCP and CAN bus interface
      for pattern control and hardware part number management.
